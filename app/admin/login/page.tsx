export default function LoginPage({ searchParams }: { searchParams: { error?: string } }) {
  return <div className="login-wrap"><form className="login-card" action="/api/auth/login" method="post"><h1>Đăng nhập Admin</h1>{searchParams?.error && <div className="login-error">Sai tài khoản hoặc mật khẩu.</div>}<input name="username" placeholder="Username" autoComplete="username" /><input name="password" placeholder="Password" type="password" autoComplete="current-password" /><button type="submit">ĐĂNG NHẬP</button><p style={{color:'#64748b',fontSize:13,lineHeight:1.6}}>Mặc định: admin / Admin@123456. Khi deploy nhớ đổi trong file .env.local.</p></form></div>;
}
